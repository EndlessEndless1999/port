---
const {title, description, badges} = Astro.props
---
<div class="card">
  <div class="card-header">
    <h4>{title}</h4>
  </div>
  <p>{description}</p>
  <div class="badges">
    {badges.map((badge: any) => (
      <span class="badge">{badge}</span>
    ))}
  </div>
  <hr>
</div>